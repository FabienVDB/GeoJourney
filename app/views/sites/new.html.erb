<%= render 'shared/navbar' %>
<div class="container mt-3">
  <div class="row">
    <div class="col-md-12">
      <h1 id=“site-title”>New Site</h1>
      <p> Hi 👋🏻 here you can add a site to <%= @itinerary.name %></p>
      <hr>
      <%= render "site_creation", locals: {itinerary: @itinerary, site: @site} %>
    </div>
  </div>
</div>
